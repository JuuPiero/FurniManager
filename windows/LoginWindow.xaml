<Window x:Class="FurniManager.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FurniManager.Windows"
        mc:Ignorable="d"
        Title="Login" 
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Form Chính -->
        <Border Grid.Row="1" Width="350" Padding="20" 
            HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Đăng Nhập" FontSize="20"  FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="Email" Margin="0,5"/>
                <TextBox x:Name="UsernameBox" Height="35" Background="#ccc" 
                     BorderThickness="0" Padding="5" />

                <TextBlock Text="Mật khẩu" Margin="0,10,0,5"/>

                <PasswordBox x:Name="PassBox"  Height="35" Background="#ccc"
            BorderThickness="0" Padding="5" />

                <Button Content="Đăng Nhập" 
                     Foreground="White" Height="40"
                    FontSize="16" Cursor="Hand" Margin="0,15,0,0"
                    BorderThickness="0" Click="OnLogin" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
