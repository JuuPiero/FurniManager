<Page x:Class="FurniManager.Screens.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FurniManager.Screens"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DashboardPage">
    <ScrollViewer>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Height="Auto"  VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" Background="CadetBlue" Margin="10">
                    <TextBlock Margin="10" FontSize="18" Text="Sản phẩm" Foreground="White" />
                    <Grid Height="80" Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Source="../res/product.png" Height="40" Width="40"  HorizontalAlignment="Left"/>
                        <TextBlock Grid.Column="1" FontWeight="Bold" Foreground="White" FontSize="20" Text="{Binding ProductCount}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                    <Border BorderBrush="White" BorderThickness="2" />
                </StackPanel>


                <StackPanel Grid.Column="1" Orientation="Vertical" Background="LawnGreen" Margin="10">
                    <TextBlock  Margin="10" FontSize="18" Text="Đơn hàng" Foreground="White" />
                    <Grid Height="80" Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Source="../res/sale.png" Height="40" Width="40"  HorizontalAlignment="Left"/>
                        <TextBlock  Grid.Column="1" FontWeight="Bold" Foreground="White" FontSize="20" Text="{Binding SaleOrderCount}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                    <Border BorderBrush="White" BorderThickness="2" />
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Vertical" Background="DarkSalmon" Margin="10">
                    <TextBlock  Margin="10" FontSize="18" Text="Tài khoản" Foreground="White" />
                    <Grid Height="80" Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Source="../res/user.png" Height="40" Width="40"  HorizontalAlignment="Left"/>
                        <TextBlock  Grid.Column="1" FontWeight="Bold" Foreground="White" FontSize="20" Text="{Binding UserCount}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                    <Border BorderBrush="White" BorderThickness="2" />
                </StackPanel>


            </Grid>
            
            <!-- Biểu đồ số lượng sản phẩm theo loại -->
            <GroupBox VerticalAlignment="Top" Header="Số lượng sản phẩm theo danh mục" MinHeight="300" Grid.Row="1" Margin="5">
                <lvc:CartesianChart Series="{Binding ProductCountByCategoryChart}">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Labels="{Binding ProductCountLabels}" />
                    </lvc:CartesianChart.AxisX>
                </lvc:CartesianChart>
            </GroupBox>

        </Grid>
    </ScrollViewer>
</Page>
